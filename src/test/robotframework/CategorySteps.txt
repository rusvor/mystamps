*** Keywords ***

# TODO: add support for name in Russian
# TODO: location after creation should be "/category/${name.toLowerCase()}"
Create Category
	[Arguments]	${name}
	Go To	${SITE_URL}/category/add
	Input Text	id=name	${name}
	Submit Form	id=add-category-form
